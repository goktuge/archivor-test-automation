/**
 * Merged fixtures for use in all test files.
 * Import: import { test, expect } from '../fixtures';
 *
 * For user factory (Gmail): import { test, expect } from '../fixtures/gmail-fixture'
 */

export { test, expect } from './page.fixtures';
